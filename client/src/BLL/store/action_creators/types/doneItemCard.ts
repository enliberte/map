import * as a from "../../constants";


export type OpenDoneItemCardAction = {
    type: typeof a.OPEN_DONE_ITEM_CARD
}

export type CloseDoneItemCardAction = {
    type: typeof a.CLOSE_DONE_ITEM_CARD
}